apiVersion: v1
kind: Service
metadata:
  name: workhub-service
spec:
  type: NodePort
  selector:
    app: workhub
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
